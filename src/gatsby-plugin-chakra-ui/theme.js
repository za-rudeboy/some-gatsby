import { theme as defaultTheme } from "@chakra-ui/core"
const theme = {
    ...defaultTheme,
    colors:{
        ...defaultTheme.colors,
        darkJungleGreen: "#031F27"
    },
};

export default theme